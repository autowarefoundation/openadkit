name: setup-docker

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'setup.sh'

jobs:
  setup-docker:
    runs-on: ubuntu-22.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run setup script
        run: |
          ./setup.sh
