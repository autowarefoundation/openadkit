!!! tip "Release bundles"
    Each stable GitHub Release publishes one `openadkit-vX.Y.Z.tar.gz` containing
    `./openadkit` and the curated deployments (planning, logging, scenario, and
    CARLA). Zenoh stays a source-checkout workflow. Images and sample data are
    pulled on demand. Until the first release exists, use a source checkout.
