{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Open AD Kit","text":"<p> The deployment layer for the Autoware ecosystem, building autonomous driving and ADAS systems from simulation and development through to in-vehicle deployment. </p> <p> First SOAFEE blueprint Cloud-native containers Simulation to in-vehicle </p>"},{"location":"#start-here","title":"Start Here","text":"<p>Try it locally</p> <p>Run a planning simulation on Ubuntu in about 10 minutes. No GPU, no build from source.</p> <p>Quickstart</p> <p></p> <p>Run a deployment</p> <p>Planning, scenario, logging, CARLA, or a distributed Zenoh bridge.</p> <p>Deployments</p> <p></p> <p>Go to hardware</p> <p>Platform profiles and verified boards, from simulation to the vehicle.</p> <p>Platforms</p>"},{"location":"#explore-the-docs","title":"Explore the Docs","text":"<ul> <li>Overview \u2014 What Open AD Kit is and how it is put together</li> <li>Components \u2014 Container images, their contents, and the build pipeline</li> <li>Container Images &amp; Versioning \u2014 Tag schema and pinning guidance</li> <li>CLI &amp; Maintenance \u2014 Runtime controls, upgrades, and cleanup</li> <li>Build from Source \u2014 Bake groups and the CI pipeline</li> <li>Contributing \u2014 How to submit changes</li> <li>Releases &amp; Roadmap \u2014 Release status and development phases</li> </ul>"},{"location":"overview/","title":"Overview","text":"<p>   What Open AD Kit is, why it exists, and how it relates to the Autoware ecosystem: the software it packages for cloud-native deployment. </p>"},{"location":"overview/#what-is-open-ad-kit","title":"What is Open AD Kit","text":"<p>Open AD Kit packages the Autoware autonomous driving stack as focused, independently deployable container images. Instead of one monolithic image, it splits the stack along the AD pipeline \u2014 sensing, perception, localization, mapping, planning, control, vehicle system, API, simulation, visualization, and the CARLA bridge \u2014 so you run only what a workload needs.</p> <p>Autoware provides the autonomy stack; Open AD Kit makes it deployable. It packages upstream software into composable images, defines deployment configurations, integrates target platforms and vehicle systems, and maintains the build, test, and release tooling that keeps simulation and in-vehicle deployments consistent.</p> <p>It also integrates Autoware Safety Island, which runs on real-time operating systems and automotive hardware rather than in a container. Open AD Kit is the first SOAFEE blueprint for the software-defined vehicle; Supported Platforms covers the derived blueprints and the platform tiers.</p>"},{"location":"overview/#why-open-ad-kit","title":"Why Open AD Kit","text":"<p>Modular Components</p> <p>Independent images for each stage of the AD pipeline. Deploy only what you need.</p> <p></p> <p>Mixed Criticality</p> <p>Separate workloads by criticality assumption across safety-qualified and standard hardware.</p> <p></p> <p>Cloud Native</p> <p>Scale from simulation to the edge with containerized deployments, reproducible image builds, and platform integrations.</p> <p></p> <p>Connected and Continuous</p> <p>CI/CD with GitHub Actions, optimized build caching, and containerized testing.</p>"},{"location":"overview/#how-it-works","title":"How It Works","text":"<p>Open AD Kit runs Autoware as a pipeline of containerized components. Each container handles one stage of autonomous driving, and the stages communicate over ROS 2 DDS on the host network.</p> <ol> <li>Sensing captures and preprocesses raw sensor data (LiDAR, camera, IMU).</li> <li>Perception detects and tracks objects, traffic lights, and drivable space.</li> <li>Mapping serves high-definition map data that the rest of the stack consumes.</li> <li>Localization determines the vehicle's exact position on the map.</li> <li>Planning computes a safe, feasible trajectory to the goal.</li> <li>Control converts that trajectory into throttle, brake, and steering commands.</li> <li>Vehicle System bridges those commands to the actual vehicle or simulator.</li> </ol> <p>A deployment runs the subset of these stages a task needs. Autoware Safety Island joins the same pipeline over DDS, adding a safety-critical actuation path alongside the containerized services.</p>"},{"location":"overview/#key-terms","title":"Key Terms","text":"Term Meaning Component image A published container image holding one or more Autoware functions, for example <code>planning-control</code>. Deployment A named, ready-to-run stack: a manifest, environment files, and Compose configuration for a task such as planning simulation. Manifest The <code>deployment.json</code> file that tells the CLI how to run a deployment: requirements, data downloads, and one-shot services. Overlay The GPU Compose file and <code>config.gpu.env</code> that <code>--gpu</code> adds on top of a deployment, such as Logging Simulation's CUDA perception. Tag alias A moving image tag such as <code>planning-control-humble</code> that follows the newest stable release. Release tags (<code>-vX.Y.Z</code>) and digests stay immutable. Digest pin An image reference by content hash (<code>@sha256:\u2026</code>) instead of a tag, so the same image bits are always used. Mixed criticality Running safety-critical and non-critical workloads in separate containers or partitions on the same hardware."},{"location":"components/","title":"Components","text":"<p>Open AD Kit packages Autoware Universe into focused container images. Each image groups closely related Autoware functions, and deployments run them as separate containers that talk over ROS 2. External clients use the Autoware AD API; the images use Autoware's component interfaces between them.</p> <p>All images build on a shared <code>universe-common</code> layer; see Build from Source for the build graph. The image reference lists tags and platforms.</p>"},{"location":"components/#component-images","title":"Component Images","text":""},{"location":"components/#sensing-perception","title":"Sensing &amp; Perception","text":"<p>Sensor preprocessing and environment understanding in one image.</p> <ul> <li>Sensing \u2014 LiDAR distortion correction, filtering, and point cloud   preprocessing; camera and radar preprocessing; GNSS/INS preprocessing; shared   point cloud container. Launch: <code>tier4_sensing_component.launch.xml</code></li> <li>Perception \u2014 camera, LiDAR, and radar object detection and fusion;   multi-object tracking and trajectory prediction; traffic light recognition;   occupancy grid mapping. Launch: <code>tier4_perception_component.launch.xml</code></li> <li>CUDA variant \u2014 <code>sensing-perception-cuda</code> accelerates point cloud   processing and neural-network inference on NVIDIA GPUs. It is amd64-only and   requires the NVIDIA Container Toolkit. Used by   Logging Simulation (<code>--gpu</code>) and   CARLA Simulation.</li> </ul>"},{"location":"components/#localization-mapping","title":"Localization &amp; Mapping","text":"<p>Serves HD maps and estimates the vehicle pose within them.</p> <ul> <li>Localization \u2014 GNSS/RTK positioning and IMU dead reckoning; visual   odometry and LiDAR map matching; automatic pose initialization; EKF state   estimation. Launch: <code>tier4_localization_component.launch.xml</code></li> <li>Mapping \u2014 Lanelet2 vector map and point cloud map serving; occupancy grid   and point cloud map construction; map coordinate transforms. Launch:   <code>tier4_map_component.launch.xml</code></li> </ul> <p>Planning and Scenario Simulation run only the <code>map</code> service; the simulator supplies map-to-odometry transforms. Logging and CARLA Simulation also run localization.</p>"},{"location":"components/#planning-control","title":"Planning &amp; Control","text":"<p>Trajectory generation and vehicle control. Deployments run them as separate containers from the same image.</p> <ul> <li>Planning \u2014 route planning on Lanelet2 road networks; behavior planning for   lanes, intersections, and obstacles; kinematically feasible motion planning;   goal and parking maneuvers; emergency fallback trajectories. Launch:   <code>tier4_planning_component.launch.xml</code></li> <li>Control \u2014 lateral steering and longitudinal velocity control; PID and   Model Predictive Control modes; vehicle-specific command conversion;   emergency stop and heartbeat monitoring. Launch:   <code>tier4_control_component.launch.xml</code></li> </ul>"},{"location":"components/#vehicle-and-system","title":"Vehicle and System","text":"<p>Vehicle actuation and system diagnostics, run as separate containers from one image.</p> <ul> <li>Vehicle \u2014 actuation and state reporting; steering, throttle, brake, gear,   and turn-signal conversion; vehicle dimensions, limits, and kinematic   parameters. Launch: <code>tier4_vehicle_launch/vehicle.launch.xml</code></li> <li>System \u2014 health monitoring and heartbeat management; diagnostic   aggregation; Minimum Risk Maneuver handling; CPU, memory, and process   monitoring. Launch: <code>tier4_system_component.launch.xml</code></li> </ul>"},{"location":"components/#api","title":"API","text":"<p>Packages the Autoware AD API used by fleet managers, HMIs, and scenario runners. It provides ROS 2 services and topics for:</p> <ul> <li>vehicle position, velocity, engage status, and operation mode</li> <li>autonomous, manual, stop, local, and remote mode transitions</li> <li>route and goal setting</li> <li>emergency stop and engage/disengage commands</li> <li>scenario simulation auto-engage and route integration</li> </ul> <p>Launch: <code>tier4_autoware_api_component.launch.xml</code>. It is the standard external entry point for the modular simulation deployments.</p>"},{"location":"components/#simulator","title":"Simulator","text":"<p>Closed-loop vehicle and environment simulation without real sensors or vehicle hardware:</p> <ul> <li>configurable kinematic and dynamic vehicle models</li> <li>dummy perception, vehicle, door, and traffic-infrastructure interfaces</li> <li>Scenario Simulator v2 adapter and localization simulation mode</li> <li>simulated point cloud preprocessing, object tracking, shape estimation, and   map-based prediction</li> <li>occupancy and elevation map handling; vehicle command conversion</li> </ul> <p>Launch: <code>tier4_simulator_component.launch.xml</code>. <code>carla-interface</code> builds on this image for CARLA-specific sensor and control translation.</p>"},{"location":"components/#visualizer","title":"Visualizer","text":"<p>Browser-accessible RViz2 through noVNC: RViz2 with Autoware plugins, Openbox, TigerVNC, and a TLS-enabled noVNC server. The VNC backend stays loopback-only, and each container creates its own self-signed certificate at startup.</p> Variable Default Values Description <code>RVIZ_CONFIG</code> <code>/opt/autoware/autoware_launch/share/autoware_launch/rviz/autoware.rviz</code> Path RViz2 configuration inside the container <code>REMOTE_DISPLAY</code> <code>true</code> <code>true</code>, <code>false</code> Use browser-based RViz2; <code>false</code> launches a local display <code>REMOTE_PASSWORD</code> \u2014 String Required when <code>REMOTE_DISPLAY=true</code> <code>WEBSOCKIFY_BIND</code> <code>127.0.0.1</code> IP address noVNC bind address; bridge networking uses <code>0.0.0.0</code> with a host loopback port mapping <code>USE_SIM_TIME</code> <code>false</code> <code>true</code>, <code>false</code> Use the ROS simulation clock <code>RVIZ_GPU</code> <code>auto</code> <code>auto</code>, <code>on</code>, <code>off</code> Automatic, forced, or disabled VirtualGL acceleration <p>For access and remote use, see the Quickstart.</p>"},{"location":"components/#carla-interface","title":"CARLA Interface","text":"<p>Packages <code>autoware_carla_interface</code>, translating Autoware control commands to CARLA and CARLA sensor data to ROS 2 messages:</p> <ul> <li>CARLA world initialization and synchronous simulation</li> <li>ego vehicle spawning and sensor-kit configuration</li> <li>camera, LiDAR, IMU, and GNSS message translation</li> <li>vehicle command calibration and traffic light state publication</li> <li>lightweight sensor mappings for constrained hosts</li> </ul> <p>Launch: <code>autoware_carla_interface.launch.xml</code>. The image is amd64-only (Humble and Jazzy); the CARLA Simulation deployment is Humble-only and needs an NVIDIA GPU.</p>"},{"location":"components/#image-reference","title":"Image Reference","text":"<p>Generated from the image catalog (<code>.github/image-inventory.json</code>), so it always matches what CI builds. Tag names are explained in Container Images &amp; Versioning.</p> Component Image ROS Distros Platforms <code>sensing-perception</code> <code>ghcr.io/autowarefoundation/openadkit:sensing-perception</code> humble, jazzy amd64, arm64 <code>sensing-perception-cuda</code> <code>ghcr.io/autowarefoundation/openadkit:sensing-perception-cuda</code> humble, jazzy amd64 <code>localization-mapping</code> <code>ghcr.io/autowarefoundation/openadkit:localization-mapping</code> humble, jazzy amd64, arm64 <code>planning-control</code> <code>ghcr.io/autowarefoundation/openadkit:planning-control</code> humble, jazzy amd64, arm64 <code>vehicle-system</code> <code>ghcr.io/autowarefoundation/openadkit:vehicle-system</code> humble, jazzy amd64, arm64 <code>api</code> <code>ghcr.io/autowarefoundation/openadkit:api</code> humble, jazzy amd64, arm64 <code>visualizer</code> <code>ghcr.io/autowarefoundation/openadkit:visualizer</code> humble, jazzy amd64, arm64 <code>simulator</code> <code>ghcr.io/autowarefoundation/openadkit:simulator</code> humble, jazzy amd64, arm64 <code>carla-interface</code> <code>ghcr.io/autowarefoundation/openadkit:carla-interface</code> humble, jazzy amd64"},{"location":"deployments/","title":"Deployments","text":"<p>A deployment is a ready-to-run Autoware stack for one task. New users should start with Planning Simulation.</p> Deployment Purpose GPU Planning Simulation Plan and follow a route on a demo map No Scenario Simulation Run predefined traffic scenarios No Logging Simulation Replay recorded sensor data through sensing, perception, and localization Recommended CARLA Simulation Drive a CARLA vehicle in closed loop Required Zenoh Bridge Split Autoware and visualization into separate ROS domains No"},{"location":"deployments/#running-a-deployment","title":"Running a Deployment","text":"<p>Complete the Quickstart setup first.</p> <p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p> <pre><code>openadkit list\nopenadkit run planning-simulation\nopenadkit status planning-simulation\nopenadkit logs planning-simulation --follow\nopenadkit stop planning-simulation\n</code></pre> <ul> <li>Add <code>--ros-distro jazzy</code> to <code>validate</code>, <code>fetch</code>, or <code>run</code> to select Jazzy;   Humble is the default.</li> <li>Add <code>--gpu</code> for GPU mode. CARLA requires it and is Humble-only.</li> <li>Put local settings in <code>config.local.env</code>; see   Configuration.</li> </ul> <p>The Zenoh bridge is not managed by the CLI. It runs from a source checkout with its own scripts; see its page.</p> <p>To build your own stack, see Custom Deployment.</p>"},{"location":"deployments/custom-deployment/","title":"Custom Deployment","text":"<p>Build your own stack by copying an existing deployment in a source checkout.</p>"},{"location":"deployments/custom-deployment/#how-deployments-are-built","title":"How Deployments Are Built","text":"<p>Each service is defined once in <code>deployments/shared/services/</code>, one file per service. A deployment directory contains:</p> File Purpose <code>docker-compose.yaml</code> Selects services with <code>include</code>, sets <code>depends_on</code>, and adds deployment-specific settings or services. It is the source of truth for the service set. <code>config.env</code> Values the selected services need, such as map paths and simulator settings. <code>config.gpu.env</code> GPU settings loaded with <code>--gpu</code>. Required when <code>deployment.json</code> lists <code>gpuFiles</code>. <code>deployment.json</code> Supported architectures, ROS distros, GPU requirement, data downloads, and one-shot services to reset on each run. <p>Container ROS and DDS settings shared by all deployments live in <code>deployments/shared/runtime.env</code>.</p>"},{"location":"deployments/custom-deployment/#start-from-planning-simulation","title":"Start from Planning Simulation","text":"<p>From the repository root:</p> <pre><code>cp -r deployments/planning-simulation deployments/my-simulation\n</code></pre> <p>In the copied <code>deployment.json</code>, set <code>name</code> to <code>my-simulation</code> and update <code>description</code>. Then register it in the <code>deployments</code> object of the root <code>openadkit.json</code>:</p> <pre><code>\"my-simulation\": {\n  \"path\": \"deployments/my-simulation\"\n}\n</code></pre>"},{"location":"deployments/custom-deployment/#customize-the-stack","title":"Customize the Stack","text":"<p>Add or remove services in <code>docker-compose.yaml</code>. A block under <code>services:</code> customizes an included service; it does not create a second container:</p> <pre><code>include:\n  # Other selected services...\n  - ../shared/services/visualizer.yaml\n\nservices:\n  visualizer:\n    depends_on:\n      - map\n</code></pre> <p>Add only the settings that differ. Deployment-only services can be defined directly in the same file. When removing a service, also check <code>depends_on</code>, <code>pid</code>, and <code>resetServices</code>: most shared services use <code>pid: service:map</code> and need the <code>map</code> service.</p> <p>Keep communicating services on the same ROS domain and middleware. For the order in which env files are loaded, see Configuration.</p>"},{"location":"deployments/custom-deployment/#validate-and-run","title":"Validate and Run","text":"<pre><code>./openadkit validate my-simulation\n./openadkit run my-simulation\n./openadkit stop my-simulation\n</code></pre> <p>Validate every distro and GPU mode you declare. Logging Simulation is an example of a GPU overlay; CARLA Simulation is an example of deployment-only services.</p>"},{"location":"deployments/carla-simulation/","title":"CARLA Simulation","text":"<p>Run the full modular Autoware stack against CARLA 0.9.16 in closed loop.</p>"},{"location":"deployments/carla-simulation/#requirements","title":"Requirements","text":"<ul> <li>amd64 host with Ubuntu 22.04 (CARLA 0.9.16 is not validated on 24.04)</li> <li>NVIDIA GPU with the Vulkan ICD at <code>/usr/share/vulkan/icd.d/nvidia_icd.json</code></li> <li>ROS 2 Humble images; this deployment does not support Jazzy</li> </ul> <p>Install Docker and the NVIDIA toolkit once, as described in the Quickstart, with GPU support:</p> <pre><code>openadkit setup --gpu --verify\n</code></pre>"},{"location":"deployments/carla-simulation/#run","title":"Run","text":"<p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p> <pre><code>openadkit run carla-simulation --gpu\n</code></pre> <p><code>run</code> downloads and verifies the Town01 map, then starts CARLA and the Autoware stack. CARLA renders offscreen by default.</p> <p><code>config/sensor_mapping.yaml</code> enables LiDAR, IMU, and GNSS. Camera entries are available but commented out.</p>"},{"location":"deployments/carla-simulation/#open-the-visualizer","title":"Open the Visualizer","text":"<p>Open <code>https://localhost:6080/vnc.html</code>, accept the self-signed certificate, and sign in with the default password <code>openadkit</code>. Set your own <code>REMOTE_PASSWORD</code> in <code>config.local.env</code>.</p> <p>For a remote host, forward the port over SSH and open <code>https://localhost:8080/vnc.html</code> locally:</p> <pre><code>ssh -L 8080:localhost:6080 &lt;user&gt;@&lt;host&gt;\n</code></pre> <p>In RViz2, set a goal with 2D Goal Pose and select Auto.</p>"},{"location":"deployments/carla-simulation/#stop-and-recover","title":"Stop and Recover","text":"<pre><code>openadkit stop carla-simulation\n</code></pre> <p>Run <code>stop</code> also after a failed start, to release the GPU. If autonomous mode is unavailable, inspect <code>/system/command_mode/availability</code> and <code>/diagnostics_graph/status</code>. For other issues, see Troubleshooting.</p>"},{"location":"deployments/logging-simulation/","title":"Logging Simulation","text":"<p>Replay a demo rosbag through sensing, perception, and localization. An NVIDIA GPU with at least 4 GB VRAM is recommended; CPU mode works but is much slower.</p> <p>Complete the Quickstart setup first. For GPU mode, run <code>openadkit setup --gpu --verify</code>.</p>"},{"location":"deployments/logging-simulation/#run","title":"Run","text":"<p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p> <pre><code>openadkit run logging-simulation         # CPU: clustering-based detection\nopenadkit run logging-simulation --gpu   # GPU: CenterPoint on CUDA (amd64 only)\n</code></pre> <p>Add <code>--ros-distro jazzy</code> to select Jazzy; Humble is the default.</p> <p><code>run</code> downloads the sample map and rosbag, and with <code>--gpu</code> also the CenterPoint models (to <code>~/autoware_data/lidar_centerpoint</code>). The rosbag starts playing once Autoware is ready.</p> <p>The demo rosbag is Copyright 2020 TIER IV, Inc. It has no camera images, so traffic-light recognition is unavailable and detection is less complete than with a full recording.</p>"},{"location":"deployments/logging-simulation/#open-the-visualizer","title":"Open the Visualizer","text":"<p>Open <code>https://localhost:6080/vnc.html</code>, accept the self-signed certificate, and sign in with the default password <code>openadkit</code>. Set your own <code>REMOTE_PASSWORD</code> in <code>config.local.env</code>.</p> <p>For a remote host, forward the port over SSH and open <code>https://localhost:8080/vnc.html</code> locally:</p> <pre><code>ssh -L 8080:localhost:6080 &lt;user&gt;@&lt;host&gt;\n</code></pre>"},{"location":"deployments/logging-simulation/#stop-and-recover","title":"Stop and Recover","text":"<pre><code>openadkit stop logging-simulation\n</code></pre> <p>To replace missing sample data, run <code>openadkit fetch logging-simulation --force</code>; it also refreshes the CenterPoint models. For other issues, see Troubleshooting.</p>"},{"location":"deployments/planning-simulation/","title":"Planning Simulation","text":"<p>Run the Autoware planning and control stack on a demo map with the built-in simulator, then set a route in browser-based RViz2. No GPU is required.</p> <p>Complete the Quickstart setup first.</p>"},{"location":"deployments/planning-simulation/#run","title":"Run","text":"<p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p> <pre><code>openadkit run planning-simulation\n</code></pre> <p>Add <code>--ros-distro jazzy</code> to select Jazzy; Humble is the default.</p> <p><code>run</code> downloads the sample map, pulls the images, and starts the stack. The demo map is Copyright 2020 TIER IV, Inc. and is provided for demonstration only.</p>"},{"location":"deployments/planning-simulation/#open-the-visualizer","title":"Open the Visualizer","text":"<p>Open <code>https://localhost:6080/vnc.html</code>, accept the self-signed certificate, and sign in with the default password <code>openadkit</code>. Set your own <code>REMOTE_PASSWORD</code> in <code>config.local.env</code>.</p> <p>For a remote host, forward the port over SSH and open <code>https://localhost:8080/vnc.html</code> locally:</p> <pre><code>ssh -L 8080:localhost:6080 &lt;user&gt;@&lt;host&gt;\n</code></pre> <p>In RViz2, set an initial pose and a goal pose, then watch the vehicle drive the route. See the Autoware planning simulation guide for details.</p>"},{"location":"deployments/planning-simulation/#stop-and-recover","title":"Stop and Recover","text":"<pre><code>openadkit stop planning-simulation\n</code></pre> <p>To replace missing or incomplete map data, run <code>openadkit fetch planning-simulation --force</code>. For other issues, see Troubleshooting.</p>"},{"location":"deployments/scenario-simulation/","title":"Scenario Simulation","text":"<p>Run predefined traffic scenarios with TIER IV Scenario Simulator. The deployment runs a scenario automatically and writes the results to the host. No GPU is required.</p> <p>Complete the Quickstart setup first.</p>"},{"location":"deployments/scenario-simulation/#run","title":"Run","text":"<p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p> <pre><code>openadkit run scenario-simulation\nopenadkit logs scenario-simulation --follow\n</code></pre> <p>Add <code>--ros-distro jazzy</code> to select Jazzy; Humble is the default.</p> <p><code>run</code> downloads the Kashiwanoha map. Autoware takes about 90 seconds to initialize; the runner then executes the scenario and writes the results to <code>deployments/scenario-simulation/output</code>.</p>"},{"location":"deployments/scenario-simulation/#open-the-visualizer","title":"Open the Visualizer","text":"<p>Open <code>https://localhost:6080/vnc.html</code>, accept the self-signed certificate, and sign in with the default password <code>openadkit</code>. Set your own <code>REMOTE_PASSWORD</code> in <code>config.local.env</code>.</p> <p>For a remote host, forward the port over SSH and open <code>https://localhost:8080/vnc.html</code> locally:</p> <pre><code>ssh -L 8080:localhost:6080 &lt;user&gt;@&lt;host&gt;\n</code></pre>"},{"location":"deployments/scenario-simulation/#configure","title":"Configure","text":"<p>Put overrides in <code>deployments/scenario-simulation/config.local.env</code>:</p> Variable Purpose Default <code>SCENARIO</code> Scenario path inside the container Bundled example <code>SCENARIO_HOST_DIR</code> Host scenario directory, mounted at <code>/scenarios</code> <code>./scenarios</code> <code>OUTPUT_HOST_PATH</code> Host results directory <code>./output</code> <code>SCENARIO_READY_TIMEOUT</code> Seconds to wait for Autoware before running <code>300</code> <code>MAP_PATH</code> Host map directory <code>~/autoware_map/kashiwanoha_map</code> <p>Relative paths resolve from <code>deployments/scenario-simulation/</code>.</p> <p>To run your own scenario, put its YAML in the scenarios directory and set, for example, <code>SCENARIO=/scenarios/my-scenario.yaml</code>. Scenarios must match the map: a custom map needs matching <code>MAP_PATH</code>, <code>LANELET2_MAP_FILE</code>, and <code>POINTCLOUD_MAP_FILE</code> values, and the planning sample map does not work here.</p> <p>Autoware parameter overrides for this deployment live in <code>config/mrm_handler.param.yaml</code> and <code>config/default_adapi.param.yaml</code>.</p>"},{"location":"deployments/scenario-simulation/#stop-and-recover","title":"Stop and Recover","text":"<pre><code>openadkit stop scenario-simulation\n</code></pre> <p>To replace missing map data, run <code>openadkit fetch scenario-simulation --force</code>. For other issues, see Troubleshooting.</p>"},{"location":"deployments/zenoh-bridge/","title":"Zenoh Bridge","text":"<p>Split Autoware and its visualization into separate ROS 2 domains and connect them with Zenoh. The edge side runs Autoware and the simulator; the cloud side runs browser-based RViz2. Both run on one host in one Compose project.</p> <pre><code>flowchart LR\n    subgraph Edge[Edge ROS_DOMAIN_ID 0]\n        A[Autoware] --&gt; EB[Zenoh Client]\n        S[Scenario Simulator] --&gt; EB\n    end\n    subgraph Cloud[Cloud ROS_DOMAIN_ID 1]\n        CB[Zenoh Router] --&gt; V[RViz2 / noVNC]\n    end\n    EB --&gt;|Internal TCP 7448| CB</code></pre> <p>This deployment needs a source checkout. It is not part of the release bundle and is not managed by <code>openadkit run</code>.</p>"},{"location":"deployments/zenoh-bridge/#setup","title":"Setup","text":"<p>From the repository root, after the Quickstart setup:</p> <pre><code>./openadkit fetch scenario-simulation   # the Kashiwanoha map\ncd deployments/zenoh-bridge\n</code></pre> <p>Set these in the shell or in <code>config.env</code>:</p> Variable Purpose Default <code>MAP_PATH</code> Kashiwanoha map directory <code>$HOME/autoware_map/kashiwanoha_map</code> <code>REMOTE_PASSWORD</code> noVNC password <code>openadkit</code> <p>Topic filters and namespaces are in <code>config/zenoh-bridge-ros2dds.json5</code>.</p>"},{"location":"deployments/zenoh-bridge/#run","title":"Run","text":"<pre><code>./cloud.sh up -d\n./edge.sh up -d            # add --no-sim to skip the scenario simulator\n</code></pre> <p>Open the visualizer at <code>https://localhost:6081/vnc.html</code> and sign in with <code>REMOTE_PASSWORD</code>.</p> <p>The Zenoh link (TCP 7448) has no authentication or encryption, so it stays inside the Compose network and is not published to the host. The bridge does not forward <code>/clock</code>; both sides use wall time.</p>"},{"location":"deployments/zenoh-bridge/#teleoperation","title":"Teleoperation","text":"<pre><code>./cloud.sh up -d --with-teleop\n./edge.sh up -d\n./run_teleop.sh\n</code></pre> <p>Start the edge with <code>--no-sim</code> to drive Autoware without the scenario simulator.</p> Key Action <code>W</code> / <code>S</code> Throttle / brake <code>A</code> / <code>D</code> Steer <code>Z</code> Toggle between <code>STOP</code> and the configured <code>operator_mode</code> (<code>REMOTE</code> by default) <code>X</code> / <code>C</code> / <code>V</code> Drive / reverse / park <code>M</code> Cycle drive mode <code>R</code> Cycle the initial-pose presets and set the pose <code>Space</code> Emergency stop or resume <code>Q</code> Quit <p>In a <code>--no-sim</code> session: press <code>R</code> to set a pose, <code>Z</code> to leave <code>STOP</code>, choose a gear with <code>X</code> or <code>C</code>, select a drive mode with <code>M</code>, then steer and accelerate.</p>"},{"location":"deployments/zenoh-bridge/#stop-and-troubleshoot","title":"Stop and Troubleshoot","text":"<pre><code>./edge.sh logs\n./cloud.sh logs\n./edge.sh down\n./cloud.sh down\n</code></pre> <p>Host port 6081 must be free. If the map is missing, run <code>./openadkit fetch scenario-simulation --force</code> from the repository root.</p>"},{"location":"development/","title":"Development","text":"<p>For contributors and maintainers:</p> <ul> <li>Build from Source: build the images locally, how CI   builds them, and how releases are cut</li> <li>Contributing: how to submit issues and pull requests</li> </ul>"},{"location":"development/build-from-source/","title":"Build from Source","text":"<p>How to build the Open AD Kit images locally, how CI builds them, and how releases are cut. Most users do not need this; pull the published images instead (see Container Images &amp; Versioning).</p>"},{"location":"development/build-from-source/#prerequisites","title":"Prerequisites","text":"<ul> <li>Ubuntu 22.04 or 24.04 (<code>openadkit setup</code> supports Ubuntu only; elsewhere,   install Docker with Buildx and Compose yourself)</li> <li>Git, <code>pipx</code>, and <code>vcs2l</code> to import the Autoware sources</li> <li>Disk space for several multi-gigabyte images</li> </ul> <p>The component Dockerfiles build on upstream Autoware images from GHCR and compile their package sets from a local <code>autoware/src</code> tree (step 4 below).</p>"},{"location":"development/build-from-source/#build-system","title":"Build System","text":"<p>Images are built with Docker Bake from <code>components/docker-bake.hcl</code>, the same file CI uses.</p> <pre><code>flowchart TB\n    CORE[\"autoware:core-devel (build)&lt;br/&gt;autoware:base (runtime)\"] --&gt; UC[\"universe-common\"]\n    CUDA[\"autoware:base-cuda-{devel,runtime}\"] --&gt; SPC[\"sensing-perception-cuda\"]\n\n    UC --&gt; SP[\"sensing-perception\"]\n    UC --&gt; LM[\"localization-mapping\"]\n    UC --&gt; PC[\"planning-control\"]\n    UC --&gt; VS[\"vehicle-system\"]\n    UC --&gt; API[\"api\"]\n    UC --&gt; VIZ[\"visualizer\"]\n    UC --&gt; SIM[\"simulator\"]\n    UC -.-&gt; SPC\n    SIM --&gt; CARLA[\"carla-interface\"]</code></pre> <p><code>universe-common</code> compiles the shared part of Autoware Universe on the upstream <code>core-devel</code> image and copies the result onto the lean upstream <code>base</code> image, so build tools never reach the runtime layers. Every component builds from it. <code>sensing-perception-cuda</code> builds on the upstream CUDA images and adds the <code>universe-common</code> install tree.</p> Group Targets Published to <code>universe-common</code> <code>universe-common-devel</code>, <code>universe-common</code> <code>ghcr.io/autowarefoundation/openadkit-common</code> <code>component</code> all component images, including the amd64-only <code>sensing-perception-cuda</code> and <code>carla-interface</code> <code>ghcr.io/autowarefoundation/openadkit</code> <code>planning</code> <code>universe-common</code> plus the images Planning Simulation needs \u2014 (local builds) <code>default</code> <code>universe-common</code> + <code>component</code> \u2014"},{"location":"development/build-from-source/#build-and-run-locally","title":"Build and Run Locally","text":"<pre><code># 1. Clone the repository\ngit clone https://github.com/autowarefoundation/openadkit.git\ncd openadkit\n\n# 2. Install host dependencies\n./openadkit setup --verify\n</code></pre> <p>Activate the Docker group membership added by <code>openadkit setup</code> before running Docker without <code>sudo</code>. Either log out and back in, or run:</p> <pre><code>newgrp docker\n</code></pre> <pre><code># 3. Make the pipx-installed vcs command available in this shell\nexport PATH=\"$HOME/.local/bin:$PATH\"\n\n# 4. Import Autoware sources at the release used by the upstream base images\nAUTOWARE_REF=1.8.0\nexport UPSTREAM_TAG=\"$AUTOWARE_REF\"\ngit clone --branch \"$AUTOWARE_REF\" --depth 1 \\\n  https://github.com/autowarefoundation/autoware.git\nmkdir -p autoware/src\nvcs import --shallow autoware/src &lt; autoware/repositories/autoware.repos\nmkdir -p autoware/src/middleware/external\ntouch autoware/src/middleware/external/.keep\n\n# 5. Build the shared universe-common layer (~2 hours)\ndocker buildx bake -f components/docker-bake.hcl universe-common\n\n# 6. Build the Planning Simulation images (~2 hours)\ndocker buildx bake -f components/docker-bake.hcl --load planning\n\n# 7. Run with the local images\n./openadkit run planning-simulation\n</code></pre> <ul> <li><code>--load</code> puts the images in the local Docker store; without it, Bake only   fills the build cache.</li> <li>Local builds are tagged <code>&lt;target&gt;-&lt;arch&gt;-&lt;ros-distro&gt;</code> (for example   <code>planning-control-amd64-humble</code>), which is the tag <code>./openadkit</code> looks up in a   source checkout. The arch follows the build host; when cross-building, set tags   with <code>--set &lt;target&gt;.tags=...</code>. To use other images, set the <code>*_IMAGE</code>   variables in <code>config.local.env</code>.</li> <li>For Jazzy, build with <code>ROS_DISTRO=jazzy</code> and run with <code>--ros-distro jazzy</code>.</li> </ul> <p>Other useful targets:</p> <pre><code>docker buildx bake -f components/docker-bake.hcl                                # everything\ndocker buildx bake -f components/docker-bake.hcl component                      # all components\ndocker buildx bake -f components/docker-bake.hcl --load sensing-perception      # one image\n</code></pre>"},{"location":"development/build-from-source/#build-variables","title":"Build Variables","text":"Variable Description Default <code>ROS_DISTRO</code> <code>humble</code> or <code>jazzy</code> <code>humble</code> <code>UPSTREAM_TAG</code> Upstream Autoware release to build on (e.g. <code>1.8.0</code>). Empty uses the moving <code>&lt;name&gt;-&lt;distro&gt;</code> tag, which is not reproducible. <code>\"\"</code> <code>UPSTREAM_REPO</code> Upstream Autoware image repository <code>ghcr.io/autowarefoundation/autoware</code>"},{"location":"development/build-from-source/#continuous-integration","title":"Continuous Integration","text":"<p><code>build-all-images.yaml</code> builds every target with the same Bake file. The build matrix comes from <code>.github/image-inventory.json</code>, the source of truth for which images are built and for which architectures.</p> <p>Jobs run in dependency order: <code>prepare</code>, then <code>build-common</code>, then <code>build-components</code>, then <code>build-carla-interface</code>. Most images build for Humble and Jazzy on amd64 and arm64. <code>create-manifests</code> then combines the per-arch images into multi-arch tags. CI disables the local tags (<code>LOCAL_IMAGE=\"\"</code>) and tags images through <code>docker/metadata-action</code>. A failed scheduled build opens a GitHub issue.</p>"},{"location":"development/build-from-source/#release-process","title":"Release Process","text":"<p>Releases promote an existing CI build; nothing is rebuilt.</p>"},{"location":"development/build-from-source/#before-you-release","title":"Before You Release","text":"<ul> <li><code>vars.UPSTREAM_TAG</code> is set in the repository or organization variables. It   selects the Autoware release to build; the workflow's <code>autoware_ref</code> input   overrides it.</li> <li>GHCR packages accept pushes from CI: <code>openadkit</code>, <code>openadkit-common</code>, and   <code>openadkit-buildcache</code> under <code>ghcr.io/autowarefoundation/</code>.</li> <li><code>secrets.RELEASE_TOKEN</code> (Contents and Workflows write) is set when promoting   a build commit older than later workflow changes; otherwise <code>GITHUB_TOKEN</code> is   used.</li> <li>A successful build of <code>build-all-images</code> exists on <code>main</code> from an exact   Autoware release tag. Builds from a full SHA can only become pre-releases. The   build summary shows its <code>build_tag</code>.</li> <li>A passing scan of <code>scan-images</code> exists for that <code>build_tag</code>.</li> </ul>"},{"location":"development/build-from-source/#workflow","title":"Workflow","text":"<p>Run <code>release.yaml</code> with the version and <code>build_tag</code>. Its jobs run in order:</p> <pre><code>flowchart LR\n    V[validate] --&gt; P[package-bundles] --&gt; T[release-tag] --&gt; D[prepare-github-release] --&gt; I[release-images] --&gt; G[release-github] --&gt; W[deploy-docs]</code></pre> <ol> <li>validate: runs the release gates in    <code>validate_release.sh</code>    (version format, build provenance and age, scan results, metadata and file    integrity, image coverage, registry digests, Git tag). Nothing is tagged if a    gate fails.</li> <li>package-bundles: writes the immutable release plan and the runtime bundle    for both distros.</li> <li>release-tag: creates or verifies the Git tag.</li> <li>prepare-github-release: creates a draft release with notes and assets.</li> <li>release-images: creates the release tags and, for stable releases, the    moving aliases (see Tag Reference).</li> <li>release-github: publishes the draft.</li> <li>deploy-docs: republishes the docs when the stable aliases moved.</li> </ol> <p>The release plan (<code>release-plan.json</code>) attached to each GitHub Release records the exact image digests, aliases, assets, and checksums of that release.</p>"},{"location":"development/contributing/","title":"Contributing","text":"<p>Open AD Kit welcomes contributions from individuals and organizations. The repository's CONTRIBUTING.md is the canonical guide for:</p> <ul> <li>Developer Certificate of Origin (DCO) sign-off</li> <li>Branch and commit conventions</li> <li>Local lint and test commands</li> <li>Deployment validation</li> <li>Release workflow</li> </ul> <p>Before opening a pull request, run the checks relevant to your change and sign every commit with <code>git commit -s</code>. Significant architectural changes should be discussed on Discord before implementation.</p>"},{"location":"development/contributing/#community","title":"Community","text":"<ul> <li>GitHub Issues \u2014 bugs and tasks</li> <li>Autoware Foundation Discord \u2014 questions, ideas, and design</li> <li>Autoware contribution guide</li> <li>Code of Conduct</li> </ul>"},{"location":"getting-started/","title":"Quickstart","text":"<p>From zero to a running Autoware planning simulation in about 10 minutes. No GPU is required.</p> <ol> <li> 1 Get the kitDownload or clone </li> <li> 2 Set up the hostConfigure dependencies </li> <li> 3 Run simulationStart planning </li> <li> 4 Open visualizerInspect the output </li> <li> 5 DriveSet a route </li> </ol>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Ubuntu 22.04 (Jammy) or 24.04 (Noble) with <code>sudo</code> access</li> <li><code>curl</code> for the release installer; <code>python3</code> and <code>tar</code> are preinstalled on supported Ubuntu releases</li> <li>A web browser - the visualizer runs in it, no display server needed</li> </ul>"},{"location":"getting-started/#get-open-ad-kit","title":"1. Get Open AD Kit","text":"<p>Release bundles</p> <p>Each stable GitHub Release will publish a versionless <code>openadkit</code> installer and one <code>openadkit-vX.Y.Z.tar.gz</code> containing the runtime entry point and the curated deployments (planning, logging, scenario, and CARLA). Zenoh stays a source-checkout workflow. Images and sample data are pulled on demand. Until the first release exists, use a source checkout.</p> Source checkoutRelease bundle <pre><code>git clone https://github.com/autowarefoundation/openadkit.git\ncd openadkit\n</code></pre> <p>Download the latest release. The command verifies the release bundle before installing it to <code>~/.local/share/openadkit</code> and makes <code>openadkit</code> available from <code>~/.local/bin</code>.</p> <pre><code>curl -fsSL https://github.com/autowarefoundation/openadkit/releases/latest/download/openadkit \\\n  | bash -s -- install\n</code></pre> <p>If <code>~/.local/bin</code> is not on your <code>PATH</code>, the installer prints the command to add it. For a reproducible install, replace <code>latest</code> with a release version and pass the same version to <code>install</code>:</p> <pre><code>curl -fsSL https://github.com/autowarefoundation/openadkit/releases/download/vX.Y.Z/openadkit \\\n  | bash -s -- install --version vX.Y.Z\n</code></pre> <p>To verify the bundle yourself before running it, see Verify a Release Bundle Manually.</p> <p>The release bundle contains only the runtime and deployment assets. A source checkout also contains the image sources, CI, and development tools.</p>"},{"location":"getting-started/#set-up-the-host","title":"2. Set Up the Host","text":"<p>After a release install, these commands work from any directory. For a source checkout, prefix them with <code>./</code> (for example <code>./openadkit</code>) and run them from the repository root.</p> <pre><code>openadkit setup --verify\n</code></pre> <p>Run setup as your normal user. It requests <code>sudo</code> only for host changes. CPU is the default; add <code>--gpu</code> for NVIDIA deployments. <code>--gpu</code> also installs NVIDIA OpenGL/Vulkan libraries needed for CARLA.</p> <p>Activate the Docker group membership added by <code>openadkit setup</code> before running Docker without <code>sudo</code>. Either log out and back in, or run:</p> <pre><code>newgrp docker\n</code></pre>"},{"location":"getting-started/#run-planning-simulation","title":"3. Run Planning Simulation","text":"<pre><code>openadkit run planning-simulation\n</code></pre> <p>Add <code>--ros-distro jazzy</code> to select Jazzy. Humble is the default in both source checkouts and release bundles.</p> <p><code>run</code> downloads and verifies the sample map, pulls missing images, starts the services, and waits until they are ready.</p>"},{"location":"getting-started/#open-the-visualizer","title":"4. Open the Visualizer","text":"<p>Wait about 10 seconds for the containers to initialize, then open:</p> <pre><code>https://localhost:6080/vnc.html\n</code></pre> <p>Use the default password <code>openadkit</code> and accept the self-signed certificate.</p> <p>For a remote host, keep noVNC loopback-only and forward it over SSH:</p> <pre><code>ssh -L 8080:localhost:6080 &lt;user&gt;@&lt;host&gt;\n</code></pre> <p>Then open <code>https://localhost:8080/vnc.html</code> locally.</p>"},{"location":"getting-started/#drive","title":"5. Drive","text":"<p>In RViz2, follow the Autoware planning simulation instructions to:</p> <ol> <li>Set an initial pose for the ego vehicle</li> <li>Set a goal pose on the map</li> <li>Watch the vehicle plan and drive the route</li> </ol>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<p>Explore the other deployments - scenario testing, rosbag replay, and CARLA, plus a standalone source-checkout workflow for distributed cloud-edge operation with Zenoh.</p> <ul> <li>CLI &amp; Maintenance - Runtime controls, validation, upgrades, and cleanup</li> <li>Components - The architecture behind what you just ran</li> <li>Container Images &amp; Versioning - Tag schema and pinning guidance</li> <li>Custom Deployment - Compose your own stack</li> </ul>"},{"location":"getting-started/cli/","title":"CLI &amp; Maintenance","text":"<p>Commands you need after the first run: runtime controls, validation, configuration, upgrades, and cleanup.</p> <p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p>"},{"location":"getting-started/cli/#runtime-controls","title":"Runtime Controls","text":"<pre><code>openadkit status planning-simulation\nopenadkit logs planning-simulation --follow\nopenadkit stop planning-simulation\n</code></pre> <p>Pass the deployment name. Without it, these commands only list what is running. One deployment runs at a time; <code>run</code> asks you to stop the current one first. After starting, <code>run</code> watches the services for 10 seconds and fails if one crashes. <code>openadkit run &lt;deployment&gt; --pull always</code> refreshes images before starting, and <code>openadkit --version</code> prints the installed version.</p>"},{"location":"getting-started/cli/#validate-before-running","title":"Validate Before Running","text":"<pre><code>openadkit validate planning-simulation --data\n</code></pre> <p><code>validate</code> checks the manifest and the Compose configuration without starting anything. <code>--data</code> also reports each data resource as <code>ok</code>, <code>missing</code>, or <code>incomplete</code> and fails on any gap. It follows the same GPU selection as <code>run</code>: use <code>openadkit validate logging-simulation --gpu --data</code> to include the CenterPoint models.</p> <p><code>list</code>, <code>version</code>, and <code>validate</code> accept <code>--json</code> for scripting.</p>"},{"location":"getting-started/cli/#configuration","title":"Configuration","text":"<p>Each deployment reads its settings from environment files in <code>deployments/&lt;name&gt;/</code>, in this order (later files win):</p> <ol> <li><code>config.env</code>: the deployment defaults</li> <li><code>config.gpu.env</code>: GPU settings, loaded only with <code>--gpu</code></li> <li><code>config.local.env</code>: your local overrides, ignored by Git</li> </ol> <p>Shell exports do not override variables defined in these files. Put host settings such as <code>MAP_PATH</code> or <code>REMOTE_PASSWORD</code> in <code>config.local.env</code>. Source checkouts can also override component images there; release images stay pinned.</p>"},{"location":"getting-started/cli/#upgrading","title":"Upgrading","text":"<pre><code>openadkit upgrade --check   # report whether a newer stable version exists\nopenadkit upgrade           # install it\n</code></pre> <p>The new release is verified and installed next to the old one, and the <code>openadkit</code> launcher is repointed. The previous version stays in the install destination (default <code>~/.local/share/openadkit/</code>), so you can roll back:</p> <pre><code>openadkit install --version vOLD --force\n</code></pre> <p><code>--force</code> replaces the kept version directory; omit it to install a version that is not kept. Source checkouts update with <code>git pull</code> instead.</p>"},{"location":"getting-started/cli/#uninstall-and-cleanup","title":"Uninstall and Cleanup","text":"<p>Stop running deployments first; <code>uninstall</code> refuses while any is running, because it removes the launcher that <code>openadkit stop</code> needs.</p> <pre><code>openadkit stop planning-simulation\nopenadkit uninstall          # keep previously installed versions\nopenadkit uninstall --all    # remove kept versions too\n</code></pre> <p><code>uninstall</code> is for release installs; remove a source checkout with Git. It also removes any <code>config.local.env</code> inside the version directory.</p> <p>Downloaded data (maps, rosbags, and perception models) lives under <code>~/autoware_map</code> and <code>~/autoware_data</code> and is kept by <code>uninstall</code>. Inspect and delete a deployment's data with:</p> <pre><code>openadkit clean planning-simulation            # report only\nopenadkit clean planning-simulation --data     # delete\n</code></pre> <p><code>clean --data</code> refuses while that deployment is running. Scenario Simulation's Kashiwanoha map is shared with the Zenoh bridge; restore it with <code>openadkit fetch scenario-simulation --force</code>.</p>"},{"location":"getting-started/cli/#verify-a-release-bundle-manually","title":"Verify a Release Bundle Manually","text":"<p>To inspect a release before running anything, download the bundle and check it against the release metadata:</p> <pre><code>VERSION=$(curl -fsSL \\\n  https://api.github.com/repos/autowarefoundation/openadkit/releases/latest \\\n  | python3 -c 'import json,sys; print(json.load(sys.stdin)[\"tag_name\"])')\ncurl -fLO \"https://github.com/autowarefoundation/openadkit/releases/download/${VERSION}/openadkit-${VERSION}.tar.gz\"\ncurl -fLO \"https://github.com/autowarefoundation/openadkit/releases/download/${VERSION}/release-metadata.json\"\nEXPECTED=$(python3 -c 'import json; print(json.load(open(\"release-metadata.json\"))[\"bundles\"][0][\"sha256\"])')\nprintf '%s  %s\\n' \"$EXPECTED\" \"openadkit-${VERSION}.tar.gz\" | sha256sum --check -\ntar -xzf \"openadkit-${VERSION}.tar.gz\"\ncd \"openadkit-${VERSION}\"\n</code></pre> <p>The extracted directory is a complete runtime; run it with <code>./openadkit</code>.</p>"},{"location":"getting-started/container-images/","title":"Container Images &amp; Versioning","text":"<p>Open AD Kit publishes images to the GitHub Container Registry (<code>ghcr.io/autowarefoundation/openadkit</code>). This page is the reference for image tags, versions, and what \"supported\" means.</p>"},{"location":"getting-started/container-images/#tag-reference","title":"Tag Reference","text":"<p>Releases are promoted from existing CI builds rather than rebuilt, so the images tested in CI are the images that ship. Every tag below points at a promoted image digest.</p> Tag Example Created Moves? Release <code>planning-control-humble-v2.0.0</code> Every release No Latest per distro <code>planning-control-humble</code> Stable releases Yes Default distro <code>planning-control</code> Stable releases, default distro only Yes Pre-release <code>planning-control-humble-v2.0.0-rc.1</code> Pre-releases No CI build <code>planning-control-humble-123456789-1</code> Every CI build (<code>&lt;run_id&gt;-&lt;run_attempt&gt;</code>) No <p>The moving aliases also exist with a <code>-latest</code> suffix (<code>planning-control-humble-latest</code>, <code>planning-control-latest</code>). Pre-releases never update them.</p> <p>Examples use Humble, the default distro. Jazzy images use the same patterns with <code>-jazzy</code> (for example <code>planning-control-jazzy</code>). <code>sensing-perception-cuda</code> and <code>carla-interface</code> are amd64-only.</p>"},{"location":"getting-started/container-images/#choosing-a-tag","title":"Choosing a Tag","text":"<ul> <li>Reproducible deployments: use a release tag (<code>vX.Y.Z</code>) or a digest.</li> <li>Latest stable for a distro: use <code>&lt;target&gt;-&lt;ros_distro&gt;</code>.</li> <li>Quick experiments: use the default alias <code>&lt;target&gt;</code>.</li> <li>Never pin per-platform CI tags such as <code>planning-control-amd64-humble</code>;   they move with every build.</li> </ul>"},{"location":"getting-started/container-images/#versioning","title":"Versioning","text":"<p>Open AD Kit uses Semantic Versioning (<code>vMAJOR.MINOR.PATCH</code>):</p> Part Increments when MAJOR A backward-incompatible change to the image set, the documented deployment layout, or a supported platform is removed. MINOR New backward-compatible capability: components, platforms, deployments, or tooling. PATCH Backward-compatible fixes: bugs, CVE remediation, image refreshes, and documentation. <p>The Open AD Kit version is independent of the Autoware version. Each release pins one upstream Autoware release, the ROS 2 distros, and every image digest, and lists them in its GitHub Release notes.</p>"},{"location":"getting-started/container-images/#ros-2-distro-support","title":"ROS 2 Distro Support","text":"Distro Status Humble Default Jazzy Published in parallel where the amd64 and arm64 builds pass <p>A release bundle carries pinned images for both distros; choose one with <code>--ros-distro</code>. A distro is supported only while it is supported upstream.</p>"},{"location":"getting-started/container-images/#what-supported-means","title":"What \"Supported\" Means","text":"<ul> <li>Releases: the latest stable release is supported. Fixes, including CVE   remediation, ship in a new release; older releases are not patched.</li> <li>Platforms: support is tiered (committed, experimental, best-effort,   unsupported). See Supported Platforms.</li> <li>No certification claims: Open AD Kit makes no safety-certification,   functional-safety, or production-readiness claims.</li> </ul> <p>Maintainers: see the Release Process.</p>"},{"location":"getting-started/troubleshooting/","title":"Troubleshooting","text":"<p>Commands on this page are shown as installed by the release installer. From a source checkout, prefix them with <code>./</code> and run them from the repository root.</p>"},{"location":"getting-started/troubleshooting/#docker","title":"Docker","text":""},{"location":"getting-started/troubleshooting/#permission-denied","title":"Permission denied","text":"<p>Your user must be in the <code>docker</code> group. Log out and back in after <code>openadkit setup</code>, or run <code>newgrp docker</code>. Do not run <code>openadkit</code> with <code>sudo</code>.</p>"},{"location":"getting-started/troubleshooting/#a-deployment-fails-to-start","title":"A deployment fails to start","text":"<ol> <li>Check that Docker is running: <code>docker info</code>.</li> <li>Check the configuration: <code>openadkit validate &lt;deployment&gt; --data</code>.</li> <li>Read the logs: <code>openadkit logs &lt;deployment&gt; --follow</code>.</li> </ol> <p>If <code>run</code> fails after the containers are created, they keep running so you can read their logs. Stop them with <code>openadkit stop &lt;deployment&gt;</code>.</p>"},{"location":"getting-started/troubleshooting/#data","title":"Data","text":""},{"location":"getting-started/troubleshooting/#missing-or-incomplete-maps-rosbags-or-models","title":"Missing or incomplete maps, rosbags, or models","text":"<pre><code>openadkit validate &lt;deployment&gt; --data\nopenadkit fetch &lt;deployment&gt; --force\n</code></pre> <p><code>validate --data</code> reports each resource as <code>ok</code>, <code>missing</code>, or <code>incomplete</code>; add <code>--gpu</code> to it for CARLA and for Logging Simulation's CenterPoint models. <code>fetch</code> always includes GPU data. The Zenoh bridge uses Scenario Simulation's map; refresh it with <code>./openadkit fetch scenario-simulation --force</code> from the repository root.</p>"},{"location":"getting-started/troubleshooting/#gpu","title":"GPU","text":""},{"location":"getting-started/troubleshooting/#nvidia-container-toolkit-not-detected","title":"NVIDIA Container Toolkit not detected","text":"<ul> <li>Install it with <code>openadkit setup --gpu --verify</code>.</li> <li>Check the GPU: <code>nvidia-smi</code> and <code>nvidia-ctk --version</code>.</li> <li>Restart Docker: <code>sudo systemctl restart docker</code>.</li> </ul>"},{"location":"getting-started/troubleshooting/#perception-is-slow","title":"Perception is slow","text":"<p>The default perception image runs on CPU. For CUDA perception, run Logging Simulation with <code>--gpu</code> on an amd64 host with an NVIDIA GPU. The CUDA image does not fall back to CPU.</p>"},{"location":"getting-started/troubleshooting/#visualizer-and-network","title":"Visualizer and Network","text":""},{"location":"getting-started/troubleshooting/#blank-visualizer","title":"Blank visualizer","text":"<p>Wait 10 to 30 seconds after <code>run</code>, then reload the page. If it stays blank, check <code>openadkit logs &lt;deployment&gt; --follow</code>.</p>"},{"location":"getting-started/troubleshooting/#port-6080-or-6081-already-in-use","title":"Port 6080 or 6081 already in use","text":"<p>Port 6080 serves the visualizer of the CLI deployments and 6081 the Zenoh bridge. Stop the service that holds the port. The CLI deployments use host networking, so adding a <code>ports:</code> mapping to <code>docker-compose.yaml</code> has no effect.</p>"},{"location":"getting-started/troubleshooting/#ros-2-nodes-on-another-machine-cannot-see-the-stack","title":"ROS 2 nodes on another machine cannot see the stack","text":"<p>DDS stays on loopback by default. To reach other hosts, set <code>CYCLONEDDS_NETWORK_INTERFACE</code> in <code>deployments/shared/runtime.env</code> to the exact LAN or VPN interface name. <code>ROS_DOMAIN_ID</code> separates traffic but provides no authentication or encryption.</p>"},{"location":"getting-started/troubleshooting/#getting-help","title":"Getting Help","text":"<ul> <li>GitHub Issues</li> <li>Autoware Foundation Discord</li> </ul>"},{"location":"platforms/","title":"Supported Platforms","text":"<p>Deployments run on Ubuntu with Docker Compose for development and simulation. Platforms are edge targets on automotive operating systems such as AutoSD.</p> <p>Open AD Kit is the first SOAFEE blueprint, co-developed with SOAFEE and the eSync Alliance; DENSO's AVP blueprint and Red Hat's AutoSD blueprint build on it.</p> <p></p> <p>AutoSD</p> <p>The upstream preview of Red Hat In-Vehicle OS. Containers run under Podman and systemd with mixed-criticality partitions.</p> <p>AutoSD</p> <p></p> <p>EWAOL</p> <p>Arm's container-centric Yocto framework and the original SOAFEE reference. Background only; not an Open AD Kit target.</p> <p>EWAOL</p>"},{"location":"platforms/#support-matrix","title":"Support Matrix","text":"<p>A path that fails its build or validation gate is dropped or marked here, not documented as if it worked. Individual boards are listed on the Hardware page.</p> Path Tier Notes Ubuntu 22.04 and 24.04 + Docker Compose deployments Committed Compose validated in CI; CARLA runs on 22.04 only Component images, amd64 + arm64 Committed <code>sensing-perception-cuda</code> and <code>carla-interface</code> are amd64-only Jazzy images Best-effort Published where the builds pass; Humble is the default CARLA deployment Experimental Humble, amd64, NVIDIA GPU AutoSD planning-simulator demo Experimental Uses upstream Autoware images, not the component images EWAOL Unsupported No assets in this repository <p>Other Linux distributions may work but are not tested.</p>"},{"location":"platforms/autosd/","title":"AutoSD","text":"<p>AutoSD (Automotive Stream Distribution) is the public, in-development preview of Red Hat In-Vehicle OS. It is built on CentOS Stream with an automotive kernel and provides:</p> <ul> <li>Mixed criticality: safety-critical containers in the root partition,   non-critical ones in an isolated QM partition</li> <li>Atomic updates: immutable OSTree images with A/B updates and rollback</li> <li>Real-time kernel for deterministic scheduling</li> <li>Container-native runtime: Podman, Quadlet (systemd container units), and   BlueChi orchestration, with no Docker daemon</li> </ul>"},{"location":"platforms/autosd/#in-this-repository","title":"In This Repository","text":"<p>AutoSD assets live under <code>platforms/autosd/</code>. Each use case contains Quadlet files for its services and Automotive Image Builder files for its OS image.</p> <ul> <li>Planning Simulator: Autoware planning and   Scenario Simulator under Podman and Quadlet</li> <li>R-Car X5H:   AutoSD 10 image for the R-Car X5H board, tested in QEMU before board bring-up</li> </ul> <p>Current scope</p> <p>These are platform demos. They run upstream Autoware images, not the modular Open AD Kit component images. Mapping the components onto AutoSD's root and QM partitions is on the roadmap.</p>"},{"location":"platforms/autosd/#building-an-autosd-image","title":"Build an Image","text":"<p>You need Docker or Podman and QEMU. Run these commands from a use-case directory such as <code>platforms/autosd/planning-simulator/</code>.</p> <p>Download the Automotive Image Builder runner and build its container:</p> <pre><code>curl -fL -o auto-image-builder.sh \\\n  \"https://gitlab.com/CentOS/automotive/src/automotive-image-builder/-/raw/main/auto-image-builder.sh?ref_type=heads\"\nchmod +x auto-image-builder.sh\nsudo bash ./auto-image-builder.sh build-builder --distro autosd10-sig\n</code></pre> <p>Build the image. The arguments are the manifest, the container image name, and the disk image to write:</p> <pre><code>sudo bash ./auto-image-builder.sh build \\\n  --distro autosd10-sig \\\n  --target qemu \\\n  --define-file aib/vars.yml \\\n  aib/image.aib.yml \\\n  localhost/awf-oak:latest \\\n  disk.qcow2\nsudo chown \"$(logname)\" disk.qcow2\n</code></pre> <p>To run Automotive Image Builder directly instead, use an RPM-based host (Fedora, CentOS, or RHEL) with Automotive Image Builder, OSBuild, and QEMU installed.</p>"},{"location":"platforms/autosd/#run-the-image","title":"Run the Image","text":"<p>With <code>air</code> installed:</p> <pre><code>air disk.qcow2\n</code></pre> <p>Or with QEMU directly:</p> <pre><code>/usr/bin/qemu-system-x86_64 \\\n  -drive file=/usr/share/OVMF/OVMF_CODE.fd,if=pflash,format=raw,unit=0,readonly=on \\\n  -drive file=/usr/share/OVMF/OVMF_VARS.fd,if=pflash,format=raw,unit=1,snapshot=on,readonly=off \\\n  -smp 20 \\\n  -nographic \\\n  -enable-kvm \\\n  -m 16384 \\\n  -machine q35 \\\n  -cpu host \\\n  -device virtio-net-pci,netdev=n0,mac=FE:00:e2:0d:ba:4d \\\n  -netdev user,id=n0,net=10.0.2.0/24,hostfwd=tcp::2222-:22 \\\n  -drive file=disk.qcow2,index=0,media=disk,format=qcow2,if=virtio,id=rootdisk,snapshot=off\n</code></pre> <p><code>-m 16384</code> (16 GB) is the minimum for the Autoware stack; use <code>-m 32768</code> for heavier workloads. <code>-m 2G</code> is enough only to boot and explore the OS.</p>"},{"location":"platforms/autosd/planning-simulator/","title":"AutoSD Planning Simulator","text":"<p>Run Autoware planning and TIER IV Scenario Simulator inside AutoSD as systemd-managed Podman containers (Quadlet). For the modular Docker Compose stack with browser RViz2, use Planning Simulation instead.</p>"},{"location":"platforms/autosd/planning-simulator/#files","title":"Files","text":"<p>Assets live under <code>platforms/autosd/planning-simulator/</code>.</p> Folder Contents <code>aib/</code> Automotive Image Builder manifest (<code>image.aib.yml</code>) and variables (<code>vars.yml</code>) <code>components/container-files/</code> Quadlet units for the containers and pod, plus the shared <code>awf-oak.env</code> <code>components/systemd/</code> Oneshot service that extracts the map <code>components/scripts/</code> Helper scripts installed into the image <p>The image pulls two pinned upstream images, not the Open AD Kit component images:</p> <ul> <li><code>ghcr.io/autowarefoundation/autoware:universe-0.45.1-amd64</code></li> <li><code>ghcr.io/tier4/scenario_simulator_v2:humble-25.0.20-runtime</code></li> </ul>"},{"location":"platforms/autosd/planning-simulator/#services","title":"Services","text":"Unit Type What it does <code>awf-oak-map.service</code> Oneshot Extracts the bundled Kashiwanoha map to <code>/opt/tier4/kashiwanoha_map</code> <code>awf-oak-planning.container</code> Container Runs <code>planning_simulator.launch.xml</code> with the map at <code>/etc/awf/map</code> and RViz disabled <code>awf-oak-simulator.container</code> Container (oneshot) Runs the bundled <code>sample.yaml</code> scenario once, then exits <code>awf-oak.pod</code> Pod Shared pod for the planning and simulator containers <p>Quadlet registers <code>.container</code> files as <code>.service</code> units, so use <code>awf-oak-planning.service</code> with <code>systemctl</code>. Both containers use <code>ROS_DOMAIN_ID=26</code> and <code>RMW_IMPLEMENTATION=rmw_cyclonedds_cpp</code> from <code>awf-oak.env</code>; use the same values for extra ROS 2 nodes.</p>"},{"location":"platforms/autosd/planning-simulator/#run","title":"Run","text":"<ol> <li>Build an AutoSD image from this    directory.</li> <li>Boot it in QEMU or on target hardware.</li> <li> <p>Check the services and follow their logs:</p> <pre><code>systemctl status awf-oak-map.service awf-oak-planning.service awf-oak-simulator.service\njournalctl -u awf-oak-planning.service -f\njournalctl -u awf-oak-simulator.service -f\n</code></pre> </li> </ol> <p>The scenario runs for a few minutes (90 s initialization plus up to 180 s). Afterwards <code>awf-oak-simulator.service</code> shows <code>inactive (dead)</code>; that is expected for a oneshot. The planning container keeps running.</p>"},{"location":"platforms/ewaol/","title":"EWAOL","text":"<p>Not an Open AD Kit target</p> <p>This page is background only. There are no EWAOL assets in this repository and no validated EWAOL deployment.</p> <p>EWAOL (Edge Workload Abstraction and Orchestration Layer) is Arm's container-centric Yocto framework and the original SOAFEE reference implementation. It is delivered as the <code>meta-ewaol</code> layer and provides Docker and K3s, with optional Xen virtualization, on edge hardware and cloud instances.</p> <ul> <li>meta-ewaol source (GitLab, archived)</li> <li>EWAOL documentation: development is now   hosted by the SOAFEE SIG</li> </ul>"},{"location":"platforms/hardware/","title":"Hardware","text":"<p>Requirements and tested hardware for Open AD Kit. Both amd64 and arm64 are supported.</p>"},{"location":"platforms/hardware/#workstation-or-cloud-instance","title":"Workstation or Cloud Instance","text":"<p>For deployments, simulation, and development:</p> Resource Minimum Recommended CPU 8 cores 16 cores RAM 16 GB 32 GB GPU None NVIDIA with 4 GB+ VRAM (for perception) Storage 50 GB 100 GB+ SSD <p>The CUDA perception image is amd64-only and does not fall back to CPU. Hosts without an NVIDIA GPU, and all arm64 hosts, use the CPU perception image.</p>"},{"location":"platforms/hardware/#edge-deployment","title":"Edge Deployment","text":"<p>Running the full Autoware stack in real time on a vehicle computer needs more. These figures come from the verified ADLINK AADP-AVA platform:</p> Resource Specification CPU 40-core Arm Neoverse N1 or equivalent RAM 32 GB Architecture arm64"},{"location":"platforms/hardware/#tested-hardware","title":"Tested Hardware","text":"Platform Architecture Status Notes ADLINK AADP-AVA arm64 (Ampere Altra) Verified Primary edge platform AWS EC2 G5.4XLarge amd64 Verified GPU cloud instance for simulation NVIDIA Jetson Orin arm64 Tests Ongoing JetPack 6 validation in progress R-Car X5H arm64 Tests Ongoing AutoSD 10 bring-up; QEMU-tested, board tests manual ADLINK ADM-AL30 arm64 Tests Ongoing No validation results recorded yet"},{"location":"releases/","title":"Releases &amp; Roadmap","text":"<p>No stable release has been published yet. Until the first release, run Open AD Kit from a source checkout.</p>"},{"location":"releases/#releases","title":"Releases","text":"<p>GitHub Releases is the canonical source for published versions, release notes, and provenance metadata. Each stable release publishes:</p> <ul> <li>a versionless <code>openadkit</code> installer</li> <li>the versioned <code>openadkit-vX.Y.Z.tar.gz</code> bundle</li> <li>the pinned Autoware meta-release, ROS 2 distro(s), and image tags and digests</li> </ul> <p>Installed releases move to the latest stable version with <code>openadkit upgrade</code>. Tag naming is documented in Container Images &amp; Versioning.</p>"},{"location":"releases/#roadmap","title":"Roadmap","text":"<p>Open AD Kit matures in four phases between May 2026 and May 2027, with a CES 2027 demo milestone.</p> Phase or milestone Status Focus Trustworthy Foundation Complete Pinned images, scans, unified bundles, compose validation, CARLA 0.9.16 Compatibility &amp; Validation In progress Lockfiles, manifests, platform matrix, Scenario V2 CI gate, health readiness, Zenoh split CES 2027 demo Milestone Flagship demo: Autoware Safety Island + CARLA Trust Signals &amp; Platform Profiles Planned SBOM, provenance, cosign signing, vulnerability policy, AutoSD/Podman profiles, BlueChi Full OTA Support Planned Staged apply, health promotion, verified rollback, end-to-end closed loop"}]}